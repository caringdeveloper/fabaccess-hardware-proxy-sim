import { iocContainer } from "./Modules/ioc-container";

export async function RegisterServices() {
  try {
  } catch (err) {
    console.log("ERROR!!!");
    console.log(err);

    process.exit(1);
  }
}
